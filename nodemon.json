{
    "watch": ["src"],
    "ext": "ts,js,json",
    "ignore": ["node_modules", "coverage", "build"],
    "exec": "yarn eslint && ts-node -r tsconfig-paths/register ./src/index.ts",
    "restartable": "rs",
    "env": {
        "NODE_ENV": "development"
    }
}